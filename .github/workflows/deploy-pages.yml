name: Deploy to GitHub Pages
on:
  push:
    branches:
      - main
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Configure Pages
        uses: actions/configure-pages@v4

      - name: Prepare public directory
        run: |
          rm -rf public || true
          mkdir -p public
          # copy all files except the .git directory
          cp -r ./* public || true

      - name: Upload artifact
        # Use the latest upload-pages-artifact version which no longer depends on deprecated upload-artifact v3
        uses: actions/upload-pages-artifact@v2
        with:
          path: public

      - name: Deploy to GitHub Pages
        uses: actions/deploy-pages@v1
